# 2026-09-03

- 完成 Learn-Workbench 全站 UI 改版设计方案：`docs/ui-redesign-proposal.md`（README 此前引用但缺失，本次补齐）。核心决策：导航 11→5 入口、三级玻璃体系、图表色板收敛（废 6 色彩虹）、仪表盘"今日焦点"三层结构、主题三档（浅/深/跟随壁纸）。用户要求先出方案、确认后再改代码。待确认后按 Phase 1~4 实施。
- 注意：ui-designer subagent 调用报 "not available"，设计方案由主会话直接完成。
- 设计文档升级 v1.1：首页新增「快捷开始」区（一键学习 25min / 一键运动 30min），点击跳 `/tasks?autofocus=study|exercise&minutes=N` 自动开始倒计时。核实结论：零 API 改动——`/api/wellbeing/exercise` 的 source 白名单已含 "FOCUS"，运动完成直接写入；前端仅改 3 处（dashboard 快捷组件、tasks 解析 searchParams、FocusTimer 加 autoStart/mode props）。运动不计专注时长，只写 exercise_logs。
- v1.1 已实施完成（团队 software-learn-workbench：engineer-2 实施 + QA 独立验证，判定 NoOne）。改动 7+2 文件：dashboard 快捷开始卡、tasks autofocus（QA 抽取 lib/autofocus.ts 纯函数）、focus-timer autoStart/mode/initialMinutes、ui-store theme 三档 light|dark|auto（persist migrate v0→v1）、globals.css 新增 .glass-1/2/3（.glass 未动）、market-charts 色板收敛、packages/ui chartSeries。测试 532/532 过、typecheck/build 过。QA 补充 10 条测试。6 项低严重度观察入 backlog（exercise done 页闪现、预设污染默认时长等）。
- 本地预览已启动：PostgreSQL（.tools/pg，首次启动需崩溃恢复约 1 分钟，须后台运行防 Ctrl+C 杀进程）+ pnpm --filter web dev @ localhost:3001。构建需先删 apps/web/.next（safe-delete 守卫，可改名绕过）。
- 用户规则：预览确认后才允许部署服务器（106.55.2.197 / learn.yuanabd.cn），禁止擅自部署。
