# Learn-Workbench 后台管理系统设计方案

> 版本：v1.0
> 日期：2026-08-12
> 状态：设计稿

---

## 一、现状分析与设计目标

### 1.1 当前设置功能分布盘点

经过对项目的完整梳理，当前"设置"相关能力分散在以下位置：

| 能力类别 | 具体功能 | 存储位置 | Web端 | 移动端 |
|---|---|---|---|---|
| **外观** | 主题（浅/深）、背景图开关 | 本地 localStorage / AsyncStorage | ✅ | ✅ |
| **账号** | 登录/登出、修改密码 | PostgreSQL (accounts/sessions) | ✅ | ✅ |
| **职业路线** | 6 种职业切换（ICT/前端/Java/数据/AI/安全） | PostgreSQL (settings 表 key=career) | ❌（仅移动端有） | ✅ |
| **数据备份** | JSON 导出/导入 | PostgreSQL 业务表 | ✅ | ❌ |
| **云同步** | 一键推送/拉取（全量替换） | PostgreSQL | ❌（占位） | ✅ |
| **数据管理** | 清空本机数据 | 本地存储 | ❌ | ✅ |
| **系统状态** | 数据库连接检测 | 运行时 | ✅ | ❌ |
| **关于** | 版本/技术栈信息 | 硬编码 | ✅ | ✅ |

**核心问题**：设置功能双端各实现一套、逻辑分散、部分能力仅单端可用（如职业路线仅移动端、数据备份仅 Web 端），且 settings 表目前仅存 career 一个键，大量 UI 偏好停留在本地存储无法跨端同步。

### 1.2 设计目标

1. **全覆盖**：将上述所有设置能力统一收敛到后台管理系统，双端不再各自实现设置页
2. **可扩展**：采用模块化 + 插件化架构，预留 P1（证书/题库/Supabase）、P2（AI）等后续功能的接入位
3. **零侵入**：不改动原项目代码，通过独立部署 + 跳转集成
4. **统一数据源**：所有设置（含 UI 偏好）入库，实现 Web/移动端真正一致

---

## 二、整体架构设计

### 2.1 系统拓扑

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户浏览器 / 移动端                        │
├──────────────┬──────────────────────────┬───────────────────────┤
│  主应用 Web   │     主应用 Mobile         │    后台管理系统 Web    │
│  (Next.js)   │     (Expo)               │    (独立 Next.js)     │
│  :3001       │                          │    :3002              │
├──────┬───────┴────────────┬─────────────┴───────────┬───────────┤
│      │  设置按钮点击       │  设置按钮点击            │  独立登录   │
│      │  → 跳转 :3002      │  → 内嵌 WebView :3002   │  页        │
└──────┼────────────────────┼─────────────────────────┼───────────┘
       │                    │                         │
       ▼                    ▼                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    统一 API 网关层 (后台系统自带)                  │
│  /api/admin/*  ──  设置管理 / 用户管理 / 数据管理 / 系统监控       │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PostgreSQL 18.4 (复用现有库)                    │
│  现有表：accounts / sessions / settings / users / 业务表...       │
│  新增表：admin_users / admin_roles / setting_categories /        │
│          setting_items / operation_logs / api_tokens / ...       │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型建议

| 层级 | 选型 | 理由 |
|---|---|---|
| 前端框架 | **Next.js 16 (App Router)** | 与主应用技术栈一致，可复用 packages/shared 和 packages/ui 的 tokens |
| UI 组件 | shadcn/ui + Tailwind + Liquid Glass 主题 | 与主应用视觉统一，直接复用现有玻璃态设计语言 |
| 状态管理 | Zustand | 与主应用一致 |
| 后端 | Next.js API Routes | 无需额外引入 Node 框架，部署简单 |
| 数据库 | 复用现有 PostgreSQL `Learn-Workbench` | 零迁移成本，直接读写同一库 |
| 认证 | 独立 JWT + 独立 admin 账号体系 | 与主应用账号隔离，后台管理员 ≠ 普通用户 |
| 部署 | 独立端口 (如 :3002) 或独立子路径 | 与主应用互不影响 |

### 2.3 与原项目的集成方式（零代码改动）

**Web 端**：
- 原项目侧边栏"设置"按钮的 `href` 从 `/settings` 改为 `http://localhost:3002`（或配置化的 `ADMIN_PANEL_URL` 环境变量）
- 跳转时携带当前用户的 session token 作为 query 参数，后台系统验证后自动登录，实现**单点登录无感跳转**
- 若不想改原项目代码，可通过 **Nginx 反向代理** 将 `/settings` 路径代理到后台系统

**移动端**：
- 原设置 Tab 改为 `react-native-webview` 内嵌后台管理系统的移动端适配页面
- 或直接用 `expo-linking` 打开系统浏览器跳转

---

## 三、功能模块设计

### 3.1 模块总览

```
后台管理系统
├── 🔐 登录认证
│   ├── 管理员账号密码登录
│   ├── 主应用 Token 单点登录（SSO）
│   └── 双因素认证（预留）
│
├── 📊 仪表盘（首页）
│   ├── 系统概览（用户数、数据量、今日活跃）
│   ├── 数据库状态 / 存储空间
│   ├── 最近操作日志
│   └── 快捷入口卡片
│
├── ⚙️ 应用设置（核心，全覆盖原设置）
│   ├── 外观设置
│   │   ├── 主题模式（浅色/深色/跟随系统）
│   │   ├── 主题色自定义（主色/强调色）
│   │   ├── 每日背景图开关
│   │   ├── 背景图源选择（Bing/自定义/纯色）
│   │   └── 背景图手动切换（历史图库）
│   │
│   ├── 职业与学习路线
│   │   ├── 当前职业路线切换（6 种）
│   │   ├── 职业路线管理（增删改，ICT 锁定）
│   │   └── 路线内容预览
│   │
│   ├── 同步与数据
│   │   ├── 云同步开关与策略（全量/增量）
│   │   ├── 一键同步到云端
│   │   ├── 从云端恢复
│   │   ├── JSON 导出备份
│   │   ├── JSON 导入恢复
│   │   ├── 数据清理（按模块选择性清空）
│   │   └── 同步历史记录
│   │
│   ├── 通知与提醒（预留）
│   │   ├── 每日学习提醒
│   │   ├── 证书倒计时提醒
│   │   └── 通知渠道（站内/邮件/Webhook）
│   │
│   └── 关于与版本
│       ├── 版本信息
│       ├── 技术栈
│       └── 更新日志
│
├── 👤 用户管理
│   ├── 用户列表（accounts 表）
│   ├── 创建/禁用/删除用户
│   ├── 重置用户密码
│   ├── 用户数据查看（进度/任务/日志）
│   └── 用户数据导出
│
├── 📚 内容管理（预留扩展）
│   ├── 路线图内容管理（阶段/主题/资源/项目）
│   ├── 职业路线内容编辑
│   ├── 面试题库管理
│   └── 证书模板管理
│
├── 🗄️ 数据管理
│   ├── 各业务表数据浏览（进度/任务/会话/日志/打卡）
│   ├── 数据统计与趋势
│   ├── 数据导出（CSV/JSON）
│   └── 数据修复工具
│
├── 🖼️ 背景图管理
│   ├── Bing 壁纸历史库浏览
│   ├── 手动上传自定义背景
│   ├── 背景图爬虫状态监控
│   └── 手动触发抓取
│
├── 🔌 API 与集成（预留）
│   ├── API Token 管理
│   ├── Webhook 配置
│   ├── Supabase 同步配置（P1）
│   └── AI 服务配置（P2，API Key/模型选择）
│
├── 📝 操作日志
│   ├── 管理员操作审计
│   ├── 登录日志
│   └── 数据变更记录
│
└── 🛠️ 系统设置
    ├── 数据库连接配置
    ├── 存储路径配置
    ├── 爬虫定时任务配置
    └── 系统维护（缓存清理/重启）
```

### 3.2 核心模块详细设计

#### 3.2.1 应用设置 → 外观设置

将原项目中分散在本地存储的 `theme` 和 `backgroundEnabled` 统一入库：

- **主题模式**：`light` / `dark` / `system`，写入 settings 表 key=`ui.theme`
- **主题色**：主色 `--primary`、强调色 `--accent`，支持预设色板 + 自定义取色器，key=`ui.accent`
- **背景图**：
  - 开关：key=`bg.enabled`
  - 来源：`bing` / `custom` / `solid`，key=`bg.source`
  - 当前选中图：key=`bg.current`（存 background_images 表 id 或自定义路径）
  - 遮罩透明度：key=`bg.overlay`
- **字体**：字号缩放、字体族选择（预留），key=`ui.fontScale`

#### 3.2.2 应用设置 → 职业与学习路线

- 复用现有 `careers` 表和 `settings.key='career'`
- 增加职业路线的**可视化管理**：列表展示各职业的阶段数、主题数、是否锁定
- ICT 路线标记为"系统锁定"，不可删除/修改内容
- 支持新增自定义职业路线（写入 careers 表 + 关联 content_phases）

#### 3.2.3 应用设置 → 同步与数据

这是合并原 Web 端"数据备份"和移动端"云同步"的核心模块：

| 功能 | 实现方式 | 对应原 API |
|---|---|---|
| 一键同步到云端 | 调用 `/api/sync/push`（需携带用户 token） | 移动端已有 |
| 从云端恢复 | 调用 `/api/sync/pull` | 移动端已有 |
| 导出 JSON 备份 | 调用 `/api/export` | Web 端已有 |
| 导入 JSON 恢复 | 调用 `/api/import` | Web 端已有 |
| 数据清理 | 按模块勾选（进度/任务/日志/打卡/证书），事务内 DELETE | 移动端"清空"增强版 |
| 同步历史 | 新增 `sync_history` 表，记录每次 push/pull 的时间、设备、数据量 | 新增 |

#### 3.2.4 用户管理

- 基于现有 `accounts` + `users` 表
- 列表展示：用户名、显示名、创建时间、最后登录、数据量统计
- 操作：创建用户（自动创建关联 user 记录）、禁用（增加 `disabled` 字段）、重置密码（管理员无需原密码）、删除（级联删除该用户所有业务数据）
- 查看用户数据：点击用户可查看其进度概览、最近任务、日志列表

---

## 四、数据库设计

### 4.1 现有表复用

| 现有表 | 用途 | 改动 |
|---|---|---|
| `accounts` | 用户账号 | 增加 `role` 字段（user/admin/superadmin）、`disabled` 字段 |
| `sessions` | 会话 | 增加 `device_type`、`ip` 字段（可选） |
| `users` | 用户信息 | 增加 `avatar_url`、`preferences` jsonb |
| `settings` | 键值设置 | **直接复用**，扩展 key 命名空间 |
| `careers` | 职业路线 | 直接复用 |
| `background_images` | 背景图 | 直接复用 |
| `app_meta` | 应用元信息 | 直接复用，存系统配置 |

### 4.2 settings 表 key 命名规范（核心优化）

当前 settings 表只有 `career` 一个 key，缺乏命名空间规划。建议采用**分层点分命名**：

```
ui.theme              → "light" | "dark" | "system"
ui.accent             → "#e8930c"
ui.fontScale          → 1.0
bg.enabled            → true
bg.source             → "bing" | "custom" | "solid"
bg.current            → 42 (background_images.id)
bg.overlay            → 0.3
career                → "ict" (已有，保持兼容)
sync.mode             → "full" | "incremental"
sync.autoPush         → false
notify.enabled        → true
notify.dailyTime      → "09:00"
ai.provider           → "openai" (P2 预留)
ai.model              → "gpt-4o" (P2 预留)
```

查询时支持前缀匹配：`SELECT * FROM settings WHERE key LIKE 'ui.%'`，方便按模块加载。

### 4.3 新增表设计

#### 4.3.1 设置分类与元数据表 `setting_definitions`

让设置项可配置化、可扩展，而不是硬编码在前端：

```sql
CREATE TABLE setting_definitions (
  id          serial PRIMARY KEY,
  key         text NOT NULL UNIQUE,          -- 对应 settings.key
  category    text NOT NULL,                 -- 'ui' | 'bg' | 'sync' | 'career' ...
  label       text NOT NULL,                 -- 显示名称
  description text,
  input_type  text NOT NULL DEFAULT 'text'   -- 'switch' | 'select' | 'color' | 'number' | 'textarea' | 'file'
              CHECK (input_type IN ('text','switch','select','color','number','textarea','file','range')),
  options     jsonb,                         -- select 类型的选项 [{value,label}]
  default_value jsonb,                       -- 默认值
  validation  jsonb,                         -- 校验规则 {min,max,pattern}
  is_public   boolean NOT NULL DEFAULT true, -- 是否对普通用户可见
  sort_order  int NOT NULL DEFAULT 0
);
```

**价值**：新增设置项只需往这张表插一条记录，前端自动渲染对应表单控件，无需改代码。这是"预留后续拓展"的关键设计。

#### 4.3.2 操作审计日志 `operation_logs`

```sql
CREATE TABLE operation_logs (
  id          bigserial PRIMARY KEY,
  admin_id    int REFERENCES accounts(id),
  action      text NOT NULL,                 -- 'login' | 'update_setting' | 'delete_user' ...
  target_type text,                          -- 'user' | 'setting' | 'data'
  target_id   text,
  detail      jsonb,                         -- 变更前后值
  ip          text,
  user_agent  text,
  created_at  timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX idx_op_logs_admin ON operation_logs(admin_id);
CREATE INDEX idx_op_logs_time ON operation_logs(created_at);
```

#### 4.3.3 同步历史 `sync_history`

```sql
CREATE TABLE sync_history (
  id           bigserial PRIMARY KEY,
  user_id      uuid REFERENCES users(id),
  direction    text NOT NULL CHECK (direction IN ('push','pull')),
  device       text,                          -- 'web' | 'mobile' | 'admin'
  data_count   jsonb,                         -- {progress:10, tasks:5, ...}
  status       text NOT NULL DEFAULT 'success' CHECK (status IN ('success','failed')),
  error_msg    text,
  created_at   timestamptz NOT NULL DEFAULT now()
);
```

#### 4.3.4 API Token 表 `api_tokens`（预留扩展）

```sql
CREATE TABLE api_tokens (
  id          serial PRIMARY KEY,
  user_id     uuid REFERENCES users(id),
  name        text NOT NULL,
  token_hash  text NOT NULL UNIQUE,
  scopes      jsonb NOT NULL DEFAULT '[]',    -- ['settings:read','data:export', ...]
  expires_at  timestamptz,
  last_used_at timestamptz,
  created_at  timestamptz NOT NULL DEFAULT now()
);
```

---

## 五、API 设计

### 5.1 API 路由规划

```
/api/admin/auth
  POST /login          → 管理员登录
  POST /logout         → 登出
  GET  /me             → 当前管理员信息
  POST /sso            → 主应用 Token 单点登录验证

/api/admin/dashboard
  GET  /overview       → 系统概览统计
  GET  /db-status      → 数据库状态

/api/admin/settings
  GET   /              → 按分类获取所有设置（含定义 + 当前值）
  GET   /:key          → 获取单个设置
  PUT   /:key          → 更新单个设置
  PUT   /batch         → 批量更新设置
  GET   /definitions   → 获取设置项定义
  POST  /definitions   → 新增设置项定义（扩展用）

/api/admin/users
  GET    /             → 用户列表（分页+搜索）
  POST   /             → 创建用户
  GET    /:id          → 用户详情
  PUT    /:id          → 更新用户信息
  DELETE /:id          → 删除用户
  POST   /:id/reset-password → 重置密码
  GET    /:id/data     → 用户数据概览

/api/admin/data
  GET   /:table        → 通用数据浏览（progress/tasks/logs/checkins...）
  DELETE /:table/:id   → 删除单条数据
  POST  /export        → 导出数据（CSV/JSON）
  POST  /import        → 导入数据
  POST  /clean         → 按模块清理数据

/api/admin/careers
  GET    /             → 职业列表
  POST   /             → 新增职业
  PUT    /:key         → 更新职业
  DELETE /:key         → 删除职业（非锁定）

/api/admin/backgrounds
  GET    /             → 背景图列表（分页）
  POST   /upload       → 上传自定义背景
  DELETE /:id          → 删除背景图
  POST   /fetch        → 手动触发 Bing 抓取

/api/admin/sync
  POST   /push         → 触发同步到云端
  POST   /pull         → 从云端拉取
  GET    /history      → 同步历史

/api/admin/logs
  GET    /operations   → 操作日志
  GET    /logins       → 登录日志

/api/admin/system
  GET    /config       → 系统配置
  PUT    /config       → 更新系统配置
  POST   /cache/clear  → 清理缓存
```

### 5.2 认证与权限

- **独立管理员账号体系**：在 `accounts` 表中通过 `role` 字段区分，`superadmin` 可管理一切，`admin` 可管理设置和内容，`user` 只能使用主应用
- **JWT 鉴权**：后台系统登录后签发独立 JWT（与主应用 session 隔离），有效期 8 小时
- **SSO 跳转**：主应用点击设置按钮时携带 `?token=<主应用session>`，后台系统验证该 token 对应用户的 role≥admin 后自动签发后台 JWT
- **API 权限中间件**：每个 `/api/admin/*` 路由校验 JWT + role 权限

---

## 六、前端页面设计

### 6.1 页面结构

```
后台管理系统
├── /login              → 登录页（液态玻璃风格，与主应用统一）
├── /                   → 仪表盘（系统概览 + 快捷入口）
├── /settings           → 应用设置（Tab 切换：外观/职业/同步/通知/关于）
├── /users              → 用户管理（表格 + 详情抽屉）
├── /data               → 数据管理（侧边栏选表 + 右侧表格）
├── /content            → 内容管理（预留，路线图/题库/证书）
├── /backgrounds        → 背景图库（网格展示 + 上传）
├── /integrations       → 集成管理（预留，API Token/Supabase/AI）
├── /logs               → 操作日志（时间线 + 筛选）
└── /system             → 系统设置
```

### 6.2 布局设计

- **左侧导航栏**：与主应用一致的毛玻璃侧边栏，图标 + 文字，分组显示
- **顶部栏**：面包屑 + 搜索框 + 管理员头像下拉（个人设置/退出）
- **内容区**：最大宽度 1200px，卡片式布局，与主应用视觉统一
- **响应式**：移动端自动切换为顶栏 + 底部导航，适配 WebView 内嵌

### 6.3 设置页核心交互

设置页采用**左侧分类 + 右侧表单**的经典后台布局：

```
┌─────────────┬──────────────────────────────────┐
│  外观设置    │  主题模式                        │
│  ● 浅色     │  ○ 浅色  ○ 深色  ○ 跟随系统      │
│  ○ 深色     │                                  │
│             │  主题色                          │
│  职业路线    │  [橙色 ▼]  [取色器]              │
│  同步与数据  │                                  │
│  通知提醒    │  每日背景图                      │
│  关于        │  [开关 开启]                     │
│             │  来源：Bing ▼                    │
│             │  当前图：[缩略图]  更换 ▼         │
│             │                                  │
│             │  [保存更改]  [恢复默认]           │
└─────────────┴──────────────────────────────────┘
```

所有设置项从 `setting_definitions` 表动态渲染，新增分类/字段只需数据库操作。

---

## 七、扩展预留设计

### 7.1 功能扩展点

| 预留模块 | 对应项目规划 | 接入方式 |
|---|---|---|
| **Supabase 云同步** | P1 M7 | 系统设置页增加 Supabase 配置（URL/anon key），同步策略切换为增量 |
| **证书管理** | P1 M8 | 内容管理 → 证书管理子页，操作 certificates 表 |
| **面试题库** | P1 M8 | 内容管理 → 题库管理子页，操作 interview_questions 表 |
| **简历资产** | P1 M8 | 内容管理 → 简历资产子页，操作 resume_assets 表 |
| **AI 功能** | P2 M9 | 集成管理 → AI 配置页，设置 API Key/模型/温度，AI 生成记录查看 |
| **多设备协作** | P2 | 用户管理 → 设备管理，查看 sessions 表活跃设备 |
| **分享进度卡片** | P2 | 内容管理 → 分享模板，配置卡片样式 |

### 7.2 架构扩展保障

1. **设置项动态化**：`setting_definitions` 表让任何新设置都能零代码上线
2. **模块化路由**：每个功能模块独立目录，新增模块只需加一个文件夹 + 注册导航
3. **通用数据 CRUD**：`/api/admin/data/:table` 通用接口，新业务表自动可管理
4. **插件化导航**：导航菜单从数据库 `admin_menus` 表读取（可新增），而非硬编码
5. **Webhook 机制**：设置变更时触发 webhook，方便后续与外部系统联动

---

## 八、优化方案

### 8.1 对原项目设置功能的优化建议

| 问题 | 优化方案 |
|---|---|
| **UI 偏好仅存本地** | 统一入库 settings 表，主应用启动时拉取覆盖本地，实现跨端一致 |
| **职业路线仅移动端可切** | 后台系统统一提供，双端共用；主应用只读展示当前职业 |
| **数据备份仅 Web 端** | 后台系统统一提供，移动端通过 WebView 也可导出 |
| **云同步是全量替换** | 后台同步设置页增加"增量同步"选项，后续升级为基于 updated_at 的增量合并 |
| **无操作审计** | 新增 operation_logs 表，所有设置变更、用户操作留痕 |
| **无同步历史** | 新增 sync_history 表，可回溯每次同步的数据量和结果 |
| **settings 表无校验** | 通过 setting_definitions 表定义 input_type 和 validation，API 层统一校验 |
| **管理员与普通用户混用** | accounts 表增加 role 字段，后台仅 admin 可进 |

### 8.2 性能优化

1. **设置缓存**：后台 API 层对 settings 表做 Redis（或内存）缓存，设置变更时失效；主应用也缓存用户设置，减少 DB 查询
2. **数据浏览分页**：`/api/admin/data/:table` 强制分页（默认 50 条/页），支持游标分页
3. **背景图懒加载**：背景图库页面用虚拟滚动 + 缩略图，原图点击后加载
4. **导出异步化**：大数据量导出改为异步任务，生成后提供下载链接，避免请求超时

### 8.3 安全优化

1. **管理员登录限流**：同一 IP 5 分钟内最多 10 次失败登录，超出锁定 15 分钟
2. **敏感操作二次确认**：删除用户、清空数据、重置密码需输入管理员密码二次验证
3. **CSRF 防护**：后台 API 校验 Origin/Referer，状态变更接口需 CSRF Token
4. **SQL 注入防护**：通用数据接口使用参数化查询，禁止拼接表名（白名单校验）
5. **密码策略**：管理员密码强制 ≥8 位 + 大小写 + 数字，定期提醒修改
6. **会话固定防护**：登录成功后重新签发 session token

### 8.4 部署与运维优化

1. **环境变量配置**：数据库连接、端口、SSO 密钥等全部通过 `.env` 配置，不硬编码
2. **健康检查端点**：`/api/health` 返回数据库状态、版本号，方便监控
3. **日志分级**：操作日志入库，应用日志写文件，错误日志单独输出
4. **备份自动化**：后台系统提供"一键全库备份"按钮，生成 pg_dump 文件供下载
5. **版本管理**：后台系统独立版本号，设置页展示与主应用的版本兼容性

---

## 九、实施路线建议

| 阶段 | 内容 | 产出 |
|---|---|---|
| **Phase 1** | 后台系统骨架 + 登录 + 应用设置（外观/职业/同步/数据/关于） | 可替换原设置页的最小可用版 |
| **Phase 2** | 用户管理 + 数据管理 + 操作日志 + 背景图管理 | 完整管理能力 |
| **Phase 3** | 设置定义动态化 + 内容管理（路线图/题库/证书） | 可扩展架构落地 |
| **Phase 4** | 集成管理（API Token/Supabase/AI 配置）+ 系统监控 | 对接 P1/P2 功能 |

---

## 十、总结

本方案的核心设计思想是**"收敛 + 动态化 + 零侵入"**：

1. **收敛**：将分散在 Web/移动端、本地/云端的所有设置能力统一到后台系统，双端设置页改为跳转，消除重复实现
2. **动态化**：通过 `setting_definitions` 元数据表 + 通用数据接口，让新增设置项和管理新业务表都不需要改代码，为 P1/P2 功能预留充足扩展空间
3. **零侵入**：后台系统独立部署、独立账号体系，通过跳转 + SSO 与原项目集成，不需要改动原项目的任何业务代码

如需进一步细化某个模块的数据库表结构、API 接口定义或页面原型，可以继续展开。
