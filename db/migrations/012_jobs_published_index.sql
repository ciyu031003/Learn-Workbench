-- ============================================================================
-- 012：招花 P1 —— 职位发布时间索引（列表按 published_at 筛选/排序加速）
-- 说明：011 → 013 历史上编号跳号（缺 12），本迁移补档并补一个缺失索引；
--       jobs 列表「发布时间」筛选（publishedWithin）与新鲜度排序依赖该列。
-- ============================================================================

CREATE INDEX IF NOT EXISTS idx_jobs_published ON job_postings(published_at DESC);
